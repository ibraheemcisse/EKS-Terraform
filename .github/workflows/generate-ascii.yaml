name: Generate ASCII artwork 

on: push

jobs:
  ascii_job:
    runs-on: ubuntu-latest  
    
    steps:
      - name: Checkout repo 
        uses: actions/checkout@v4
        
      - name: Execute script
        run: ./script.sh
      
      - name: Executing shell 
        run: ./script.sh

    #  - name: Read files  
    #    run: cat README.md
